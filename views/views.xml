<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="gestio_hospital.list">
      <field name="name">gestio_hospital list</field>
      <field name="model">gestio_hospital.gestio_hospital</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="gestio_hospital.action_window">
      <field name="name">gestio_hospital window</field>
      <field name="res_model">gestio_hospital.gestio_hospital</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="gestio_hospital.action_server">
      <field name="name">gestio_hospital server</field>
      <field name="model_id" ref="model_gestio_hospital_gestio_hospital"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="gestio_hospital" id="gestio_hospital.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="gestio_hospital.menu_1" parent="gestio_hospital.menu_root"/>
    <menuitem name="Menu 2" id="gestio_hospital.menu_2" parent="gestio_hospital.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="gestio_hospital.menu_1_list" parent="gestio_hospital.menu_1"
              action="gestio_hospital.action_window"/>
    <menuitem name="Server to list" id="gestio_hospital" parent="gestio_hospital.menu_2"
              action="gestio_hospital.action_server"/>
    -->
  </data>
</odoo>